# Production Environment Configuration for Oracle Forms & Reports 14c
# Copy this file to .env when using docker-compose.production.yml

# =============================================================================
# VNC Configuration
# =============================================================================
VNC_PORT=5901
VNC_RESOLUTION=1280x1024
VNC_PASSWORD=Oracle123

# =============================================================================
# Oracle Forms & Reports Ports
# =============================================================================
ADMIN_PORT=7001        # WebLogic Admin Console
FORMS_PORT=9001        # Forms Server
REPORTS_PORT=9002      # Reports Server (maps to container port 9012)
WLS_PORT1=5556         # WebLogic additional ports
WLS_PORT2=5557

# =============================================================================
# Oracle Database Ports
# =============================================================================
DB_PORT=1521           # Oracle Database listener

# =============================================================================
# Nginx Reverse Proxy Ports (Optional - use --profile proxy)
# =============================================================================
PROXY_HTTP_PORT=8880
PROXY_HTTPS_PORT=8443

# =============================================================================
# Resource Limits for Forms & Reports Container
# =============================================================================
# Shared memory for Oracle (minimum 2GB recommended)
SHM_SIZE=2gb

# Memory limits (adjust based on your host system)
# Recommended minimum: 8GB reserved, 12GB limit
# For production: 12GB reserved, 16GB limit
MEM_LIMIT=12g
MEM_RESERVATION=8g

# CPU allocation (number of CPUs to allocate)
CPUS=4

# =============================================================================
# Database Resource Limits
# =============================================================================
DB_SHM_SIZE=1gb

# =============================================================================
# WebLogic Credentials
# =============================================================================
# IMPORTANT: Change these in production!
WLS_USER=weblogic
WLS_PW=Oracle123

# =============================================================================
# Oracle Database Configuration
# =============================================================================
# Database credentials
DB_PASSWORD=Oracle123
DB_APP_USER=rcu_user
DB_APP_PASSWORD=Oracle123
DB_SERVICE=FREEPDB1

# =============================================================================
# Auto-Start Configuration
# =============================================================================
# Automatically start Oracle services when container starts
# Set to 'false' to disable auto-start (manual VNC access only)
AUTO_START_SERVICES=true

# =============================================================================
# Connection Information
# =============================================================================
# VNC Access: vnc://localhost:5901 (password: Oracle123)
# WebLogic Console: http://localhost:7001/console
# Enterprise Manager: http://localhost:7001/em
# Forms: http://localhost:9001/forms/frmservlet
# Reports: http://localhost:9002/reports/rwservlet
# Oracle DB: localhost:1521/FREEPDB1

# With Nginx Proxy (--profile proxy):
# Admin Console: http://localhost:8880/console
# Forms: http://localhost:8880/forms
# Reports: http://localhost:8880/reports

# =============================================================================
# Configuration Files Location
# =============================================================================
# Forms Config: ./config/forms/
# Reports Config: ./config/reports/
# TNS Names: ./config/tnsnames/
# Forms Source: ./forms_source/
# Reports Source: ./reports_source/
# Logs: ./logs/

# =============================================================================
# Important Notes
# =============================================================================
# 1. The Oracle installation is baked into the Docker image
# 2. Only configuration files are mounted externally for customization
# 3. Database runs in a separate container (oracle-db)
# 4. All services auto-start by default
# 5. First build will take 10-15 minutes (copies Oracle folder into image)
# 6. Subsequent starts take ~3-5 minutes for all services to be ready
